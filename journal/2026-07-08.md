# 2026-07-08

## R3.1 官方结果回填

- 用户回报 R3.1 flash-attn prefill candidate 的官方平台结果：`AC`，最终得分 `74.6924`。
- 官方三档实际吞吐：4K-8K `13.78`，8K-16K `12.89`，16K-32K `11.18`。按 `0.2/0.5/0.3` 权重的吞吐 proxy 为 `12.555`。
- 扣分：SLA `0.0`，精度 `0.5644`；若无其他隐藏扣分，扣精度前约 `75.2568`。
- 与本地 R3.1 guard 对比：本地 `13.416425 / 11.455965 / 8.454834`，官方三档均更高，尤其 16K-32K。结论：本地 proxy 对 R3.1 throughput 偏保守。
- 落盘 `experiments/r3.1-official-ac-20260708/`，并更新 `memory/50-arch-bottleneck.md`、`plans/r2-r5-detail.md`、`plans/roadmap.md`。
- 决策：R3.1 现在是当前官方安全 baseline；战略动作 A（第二个官方分校准）已完成。下一主线保持 R3.0/R2.4 GEMM autotune，因为 post-R3.1 profile 已显示 GEMM 主导。

## R3.0 继续执行检查

- 用户询问是否可以继续执行剩余任务书、是否需要调整。结论：可以继续，但执行顺序要以 R3.1 official AC 为当前 baseline，先做 `task-r3.0-gemm-autotune.md` Step0 shape attribution，再做 TunableOp/Inductor A/B。
- 调整点：R3.2 GDN 维持降级；R4 INT8 暂不抢跑；后续候选对齐 R3.1 official AC 的三档吞吐与 `0.5644` 精度扣分，不再只看旧 d29 `59.0018`。
- 尝试检查 SCNet 容器：`scnetctl` 返回 `job: none`, `worker: unreachable`, `Chrome startup is required`。因此未启动 vLLM/profiler/guard 长任务。
- 已落盘 handoff：`experiments/r3.0-shape-attribution-blocked-20260708-1051/`，下一步是启动容器后按 `plans/task-r3.0-gemm-autotune.md` Step0 执行。
- 用户要求按计划书制定新计划和目标后继续执行。Codex 创建目标：执行 R3.0/R2.4 GEMM autotune Step0 shape attribution；执行计划写回同一 handoff 目录。再次检查容器仍为 `job: none`，`scnetctl attach` 在无 job 状态下阻塞，已中断；没有远端任务残留、没有修改比赛源码。

## R3.0 Step0 shape attribution

- 用户手动启动新容器后，Codex 直接使用 generated SSH config 的 worker IP，绕过卡住的 `scnetctl attach`；安装 competition wheel 并用 locked `runai_streamer` 服务口径启动诊断。
- 第一次尝试把 competition 源码目录 prepend 到 `PYTHONPATH`，启动失败：源码路径绕过 wheel 注册的 C 扩展，触发 `torch.ops._C.silu_and_mul` 缺失。改为 installed competition wheel + probe path 后服务正常启动。
- 成功 run：`/public/home/xdzs2026_c166/codex_runs/r3.0-gemm-shape-attribution-20260708-112017-wheel`。Driver 的 900s health timeout 先写 `.exit=24`，但服务随后 ready；补救脚本复用热服务发 8-16K first prompt `max_tokens=1`，status `200`，wall `127.414s`，`.salvage_exit=0`，并关闭服务。
- 结论落盘 `experiments/r3.0-gemm-shape-attribution-20260708-112017-wheel/`：Python hook 只抓到 visual tower warmup shapes，不能作为 language Cijk 归因；改用 torch compile cache `computation_graph.py` + `config.json` 静态归因。
- Compiled graph projection families：MLP `gate_up_proj [34816,5120]` 64层、MLP `down_proj [5120,17408]` 64层、GDN `in_proj_qkvz [16384,5120]` 48层、GDN `in_proj_ba [96,5120]` 48层、GDN `out_proj [5120,6144]` 48层、full-attn `qkv_proj [14336,5120]` 16层、full-attn `o_proj [5120,6144]` 16层。
- Call-count reconciliation：4 个 prefill chunks × language projections = `1216`，加 logits/lm_head `4` = `1220`，与 R3.2 hipprof Cijk calls 完全一致。Top WGM row 到 exact shape 仍需 per-call M/N/K 或 TunableOp record；Step1 先跑 TunableOp 8-16K throughput-only，必须确认 result file 覆盖上述 shape，否则关闭 TunableOp 转 Inductor GEMM autotune。
