# 2026-07-05

## 23:24 — opencode 接入会话
- 用户让读取当前文件夹信息并借助 codex/claude 留下的信息。
- 读 Claude 私有 memory 4 份（`MEMORY.md` / `keynary-background.md` / `xiandaobei-competition-overview.md` / `xiandaobei-env-workflow.md`）。
- 派 general 子代理通读 Codex rollout `2026-07-05T19-55-09` 与 `2026-07-05T22-36-25`，提炼 SSH 三段跳/容器状态/优化链/数字/中断点。
- 关键修正：Claude memory 里"容器 SSH 不对公网开放"已作废 —— 实测经 `zzeshell.scnet.cn:65032 → e03r1n07 → root@容器` 可达，`~/.ssh/config` 里别名 `xiandaobei-worker` 已配好。

## 23:48 — 探本月运行容器
- 当前 Slurm 作业 `653750`（`hx1hdexclu08`，start 22:37:43，End 02:37:43，4h 窗口）。
- 容器 `9f9ddd4c156d` 名 `653750_e03r1n07`，IP `173.0.59.2`（不是上次 Codex 记的 .6 或 .7；每次重建必变）。
- 同节点 `e03r1n07` 上还跑着 xdzs2026_c291 建的 6 个容器（image `qwen3.5-dtk26.04:0509`、onescience 等），不是我们的，无需关注。
- 容器内状态：
  - vLLM 已装 `0.18.1`（site-packages @22:54）。
  - `start_vllm.sh` 当前**未跑**，8001 无服务（`curl 8001/health` 返回 Squid 报错页 → 提示代理陷阱）。
  - 家目录 `/public/home/xdzs2026_c166/`：`vllm_cscc/dist/*.whl` 与 `vllm_cscc_competition/dist/*.whl` 都在；模型 `Qwen3.5-27B` 52G 齐全；testdata 全套在。

## 痛点讨论
- 用户提"每次启动前置工作一大堆"。
- opencode 给出最小前置 5 步：重定位 IP / 重装 wheel / unset 代理 / 起 `start_vllm.sh` / 健康检查。理论下限 ≈ 7–12 min，其中 5–10 min 是 vLLM 载入 27B 权重（物理下限），其余可压到 1–2 min。
- 可跳过：52G 模型 `cp` 到 `/root`（`start_vllm.sh` 默认读家目录）；`bdist_wheel` 重编（dist 持久）。

## 决定建共享 meta 仓
- 用户同时用 Codex / Claude / OpenCode → 需要项目内 git 仓当单一真相源。
- 设计：`memory/` 长期事实、`journal/` append 时间线、`experiments/<id>/` 实验账、`snapshots/<id>/` 工件指针（不落数据本体）。
- GitHub 私仓：`stoic-yang/xiandaobei_qwen3.5`。本地 `/Users/keynary/Code/xiandaobei/meta/`，容器 `/public/home/xdzs2026_c166/meta/`。
- 目录名定为 `meta`（账号专用，不需要 xiandaobei 标识）。

## 待办（接续 Codex 中断点）
1. 把 meta 仓骨架 init 并推 GitHub。
2. 容器内 `git clone` 一份到 `/public/home/xdzs2026_c166/meta/`。
3. 修 model loader 假阳性：在隔离 worktree 临时绕 strict-check 验证精度，再决定修正方案。
4. 解决完 → 起 `start_vllm.sh` → `run_throughput.sh all 10` → `run_accuracy.sh` → 按官方公式重算分。
5. 群里确认 EM vs F1。
6. 关注平台私钥 7-20 过期，提前续签。