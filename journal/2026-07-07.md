# 2026-07-07

## 00:00-01:35 Codex R1 overlay guards

- SCNet container was rebuilt as job `656013` on node `e03r1n05`, IP `173.0.90.4`; regenerated SSH config through `scripts/scnetctl.py attach` and continued using the ControlMaster-generated config.
- Started `guard-d29e9db3-overlay-fullsmoke-20260706-2355` with an explicit d29 source overlay. Runtime fingerprint changed `triton_unified_attention.py` from the wheel/a55 SHA `8e8d393c...` to the d29 SHA `acf4b51...`, and vLLM reached `/health`.
- Stopped the d29 overlay run during `4-8K rep=1` after an abnormal first warmup request of 128.30s. Verdict: partial negative-suspicion only; no full three-bucket median, so it is not a completed R1 sign-table row.
- Ran `guard-a55f3c3-overlay-fullsmoke-20260707-0010` to completion with the R0.3 guard protocol: warmup, 3 buckets x 3 repetitions, 10 prompts, smoke accuracy.
- a55 medians: 4-8K 12.156717 tok/s, 8-16K 7.231679 tok/s, 16-32K 4.655501 tok/s, weighted 7.443833. Compared with the earlier installed-wheel guard, deltas are -0.58%, +0.02%, +0.09%, weighted -0.16%.
- Smoke accuracy log reports hotpotqa 100.00, gov_report 30.51, retrieval recalculated 100.00 (1/1), aggregation recalculated 100.00 (1/1). The aggregation result JSON itself stores `accuracy: 0.0`; treat this as a mouthpiece/evaluator question until manually confirmed.
- Pruned old copied `accuracy/output/local_accuracy_qwen35/*` directories from the a55 experiment, leaving only this run's `20260707_010616` output plus raw logs and summary.

## Current R1 state

- Complete guard row available: `a55f3c3` overlay / current installed-wheel-equivalent runtime.
- Partial evidence only: `d29e9db3` source overlay, because it was interrupted before the full guard.
- Still incomplete: strict 6+1 commit sign table for `33323a1`, `993a944`, `0ba4953`, `293566c`, `fde463d`, `a55f3c3`, `d29e9db3`.
- Functional loader boundary from history remains: `torch.empty` repair enters at `293566c`; `flatten(1)` enters at `fde463d`. Stop-loss rollback must preserve the loader-required path rather than reverting blindly to pre-loader commits.

## 01:35-09:15 Codex R1 continuation

- Completed `guard-d29e9db3-overlay-fullsmoke10-20260707-0122` on job `656013` with d29 source overlay active (`triton_unified_attention.py` site SHA `acf4b51...`). Medians: 12.223197 / 7.230687 / 4.655347 output tok/s, weighted 7.456587. Smoke10 accuracy: hotpotqa 67.71, gov_report 35.09, retrieval 100.00 (10/10), aggregation 100.00 (10/10). Verdict: d29 source path is not the suspected large regression; relative to same-container a55 it is +0.171% weighted but has tiny long-bucket negatives.
- Completed `guard-fde463d-overlay-fullsmoke10-20260707-0236` before job `656013` timed out. Medians: 12.230124 / 7.232895 / 4.655773 output tok/s, weighted 7.459204. Smoke10 accuracy: hotpotqa 67.71, gov_report 35.00, retrieval 100.00 (10/10), aggregation 100.00 (10/10). Verdict: fde is small positive versus same-container a55 (+0.207% weighted) and also slightly above d29 (+0.035% weighted); d29 attention remains a tiny negative versus fde (-0.035% weighted), not a large loss.
- Prepared isolated stop-loss candidates without touching teammate `vllm_cscc_competition`: `a55-revert333` at `8f3df5c3e512531a992865962c53a43968df7ef9` and `d29-revert333` at `51cb6f325ab53854e94f5d4b5018712f4f662d7f`. Both remove the 333 GDN long-prefill chunk path while preserving later loader/out-buffer changes.
- Container pool correction: direct `sbatch` replay of old SubmitLine produced job `656380`, which completed in 15s with `_dockerlist_656380 does not exist` and no container. Chrome UI start of stopped instance `Instances_2607051832494555` produced job `656384`, RUNNING on `e03r1n11`; `scnetctl.py attach` resolved IP `173.0.195.9`. Evidence saved in `experiments/r1-container-start-20260707-0854/`.
- Started new same-container baseline `guard-d29e9db3-overlay-fullsmoke10-20260707-0900` on job `656384`; as of 09:12 it is still copying the model to `/root/Qwen3.5-27B.tmp` (22G copied). This run is intended as the local baseline for the `d29-revert333` candidate.

## 10:30-11:30 opencode 仓库收尾 pass（纯本地，未 push GitHub）

User 让 opencode 把 `meta-r0-r1` 仓库的未跟踪/未提交状态凝固进 git，给 Codex 之后远程同步留一个干净点的工作面。文档落点：本 journal 条目是时间线条目；worktree 拓扑、scripts 权威版本、远程阻塞、7 个 R1 experiment 目录的处置记录已写入 `memory/30-codeworkflow.md`（changelog 行 + dated 新节），不另开新文件，遵循 AGENTS.md "never mix" 规则。

- 复核 `meta` / `meta-r0-r1` / `meta-infra-verify` 的 worktree 关系：同一仓库三条 codex/* 分支；`infra-verify` 祖先含 `r0-r1`，`scnet-submit-auto` 与 `r0-r1` 自 `41cbafb` 分叉但内容不重叠，可干净 merge。
- 把 `meta-r0-r1` 上 Codex 早先留在工作树里的 dirty 改动按 AGENTS.md "1 exp = 1 commit" 切分 commit：`72fee79 infra(scripts): overlay-source-dir + bg poll + pool gate`、`5cbeadc journal: append 01:35-09:15 R1 continuation`、`6ded552..2a96294` 共 7 个 experiment commit（详见 `memory/30-codeworkflow.md` 处置记录）。
- hotserver-nooverlay-1011 没有 README，opencode 写了 stub 标明 partial + 末态 8-16K rep=3 + run config；明确注 "Codex owns the final verdict"，等原作者接手补 metrics。
- **未动** `meta` / `meta-infra-verify` 两个 worktree 的 dirty 改动（`scnetctl.py` / `chrome_submit_adapter.mjs` / `submit_job.py` / `automation/submission.json`），按 worktree owner=它们各自的 agent 处理；只保留快照不动。
- **未推送 GitHub**（用户未要求）。
- 远程阻塞待 Codex 修一次：容器侧 `.git/FETCH_HEAD` root-owned，`git pull` 被拒；见 `memory/30-codeworkflow.md` 修法。

## 12:40-12:48 Codex R1 same-container d29 vs revert333

- Finished `guard-d29e9db3-hotserver-nooverlay-fullsmoke10-20260707-1011` on job `656384` as the same-container d29 baseline for rollback testing. The run reused the hot d29 server created by `guard-d29e9db3-overlay-fullsmoke10-20260707-0900` and avoided a new git overlay because NFS git-object access had already stalled twice. Runtime fingerprint confirms `triton_unified_attention.py` site SHA `acf4b51...` vs wheel SHA `8e8d393...`.
- 1011 medians: 4-8K `12.211258`, 8-16K `7.223185`, 16-32K `4.652457` output tok/s, weighted `7.449581`. Smoke10 accuracy: hotpotqa `67.71`, gov_report `35.00`, retrieval `100.00 (10/10)`, aggregation `100.00 (10/10)`.
- Ran rollback candidate `guard-d29-revert333-srcdir-fullsmoke10-20260707-1126` from isolated source head `51cb6f325ab53854e94f5d4b5018712f4f662d7f`, overlaying from `/root/overlay-d29-revert333-51cb6f325ab5` to avoid NFS git-object stalls.
- 1126 candidate medians: 4-8K `12.209453`, 8-16K `7.219372`, 16-32K `4.644872` output tok/s, weighted `7.445038`. Relative to 1011 baseline: `-0.0148%`, `-0.0528%`, `-0.1630%`, weighted `-0.0610%`.
- Candidate accuracy preserved within smoke noise: hotpotqa `67.71`, gov_report `35.30`, retrieval `100.00 (10/10)`, aggregation `100.00 (10/10)`.
- Verdict: `d29-revert333` is not baseline-safe by the R1 throughput criterion because all three buckets are lower than the same-container d29 baseline. Do not use `51cb6f3...` as the stop-loss candidate without another independent confirmation run that reverses this sign.
- Tooling correction from this block: `guard_bench.py` now defaults to remote-background execution, uses short git metadata timeouts, supports `--overlay-source-dir`, and skips copying full `accuracy_debug/output` unless `--copy-accuracy-output` is explicitly set. This avoids SSH SIGHUP killing long guards and avoids NFS stalls on large accuracy-output directory copies.

## 12:58-13:39 Codex RunAI streamer startup probes

- Started fresh SCNet job `656918` on node `e03r1n12`, IP `173.0.253.4`, after job `656384` reached its 4h limit. `scripts/scnetctl.py attach` regenerated the multiplexed SSH config and confirmed worker reachability.
- Tested the user-provided `--load-format runai_streamer` startup shape in `experiments/runai-startup-probe-fresh-20260707-1259/`. The mechanism is Run:ai Model Streamer parallel/streaming safetensors loading, not just "avoid copy"; however the copied script omitted the locked `--max-model-len 32768`, so vLLM used `max_seq_len=262144`. It streamed `51.7 GiB` in `434.77s` and spent `429.97s` in torch.compile, but `/health` was still not ready after 21 minutes. Verdict: invalid for competition timing, useful as a negative control.
- Reran a compliant startup probe in `experiments/runai-startup-probe-locked-20260707-1324/` with `--max-model-len 32768`, `--max-num-seqs 128`, `--max-num-batched-tokens 4096`, the competition wheel, home-directory `MODEL_DIR`, and shared `VLLM_CACHE_ROOT` / `TRITON_CACHE_DIR`.
- Compliant run reached `/health` at `2026-07-07T13:36:57+08:00`, `725s` after `vllm serve` start. Key split: RunAI streamed `51.7 GiB` in `341.16s` (`155.3 MiB/s`), model loading `342.118507s`, torch.compile `44.08s`, profiling/warmup `171.88s`, graph capture `42s`, engine init after load `279.43s`.
- Verdict: this is a good fast-iteration startup path when `/root/Qwen3.5-27B` local copy is absent, because it avoids the 40-60 minute explicit model-copy workflow. It does not prove a 300s full-ready startup and must stay separate from R1 commit throughput A/B.
- Follow-up guard correction: local search of archived `vllm_server.log` files found old guard launches resolving `max_seq_len=262144` because remote `testdata/start_vllm.sh` lacks locked `--max-model-len 32768`. Updated `scripts/guard_bench.py` to add `--locked-start-script` and optional `--load-format`; future R1 measurements must use `--locked-start-script --load-format runai_streamer` and verify `max_seq_len=32768` in the raw log.

## 13:44-14:32 Codex locked guard attempt and R1 pause

- Tried to start a new d29 locked-start guard as a corrected R1 baseline. First attempt `experiments/guard-d29e9db3-locked-runai-fullsmoke10-20260707-1344/` used `--overlay-rev d29e9db3`; it was stopped before vLLM startup because `git show d29e9db3:...` stalled on the shared filesystem. Prepared `/root/overlay-d29e9db3-locked` with `git archive` instead.
- Second attempt `experiments/guard-d29e9db3-locked-srcdir-fullsmoke10-20260707-1351/` applied the `/root` overlay but failed before vLLM startup due to a generated remote-script heredoc indentation bug triggered by multi-line `--env` exports. Fixed in commit `90459ea` (`fix(guard): preserve heredoc dedent with env exports`).
- Third attempt `experiments/guard-d29e9db3-locked-srcdir-fullsmoke10-20260707-1355/` successfully validated the corrected locked startup path: vLLM log has `max_model_len=32768`, `max_seq_len=32768`, and `load_format=runai_streamer`; RunAI streamed `51.7 GiB` in `303.38s`; service reached ready at `2026-07-07T14:05:52+08:00`.
- 1355 completed only 4-8K reps before the user reprioritized away from remaining R1 commit A/B. 4-8K output tok/s: `12.032690`, `12.258413`, `12.256351`; median `12.256351`. It was stopped during 8-16K rep1, so it is not a sign-table row.
- User direction at this point: pause remaining R1 commit measurements (`33323a1 / 993a944 / 0ba4953 / 293566c`) because the expected score impact is small; focus next on higher-ROI work.

## 14:40 Codex skill sedimentation

- User confirmed the locked `runai_streamer` startup path is usable and should be kept. Added `.claude/skills/xiandaobei-operator/` to the R0/R1 meta branch, using the existing fast-iteration skill as the base and adding the validated locked startup rule.
- The skill now makes `scripts/guard_bench.py --locked-start-script --load-format runai_streamer` the default rebuilt-container guard entry, with shared `VLLM_CACHE_ROOT` / `TRITON_CACHE_DIR`, smoke10 default, `/root/overlay-<rev>-locked` source overlays, and mandatory log verification for `max_seq_len=32768` / `load_format=runai_streamer`.
- Updated `AGENTS.md` so future agents load the project skill before remote experiments and do not accidentally use remote `testdata/start_vllm.sh` or the invalid no-`--max-model-len` command as official guard口径.

## ~晚 Claude R2–R5 细化设计会话

- 通读 main 最新（memory 20/30/50、roadmap、infra-pool、journal 07-07、R0/R1 experiments 结论）。
- 吸收 R0/R1 硬结论：架构 64=48GDN+16full 非MoE gfx936；roofline HBM 1206GB/s / 395TFLOPS；
  profile 靶心 unified_attention 38.82% + GDN chunk ~30%；decode 带宽到顶（GEMV 92–101%，量化=红线封死）。
- **新增分析增量**：decode TPOT 69ms vs 权重物理下限 ~45ms → **~24ms(≈30–35%) 是 host/launch gap**，
  这是 R2 能抢的天花板（splitting_ops 打断 cudagraph → bs=1 每 tok 大量 launch 的嫌疑）。
- **产出 `plans/r2-r5-detail.md`**：R2 重定位为抢 decode host gap（R2.0 decode-only trace → R2.1 cudagraph 覆盖度
  → R2.3 host 重叠 → R2.4 GEMM 库）；R3 flash-attention on gfx936（head_dim=256 是难点+护城河）+ GDN prefill；
  R4 降级为 R3 的可选精度增强（量化被规则+架构双重封死，诚实说明）；R5 补 SLA 完成率专项。
- **两个战略动作**（价值高于剩余 R1）：A 提交第二个官方分校准"本地Δ↔官方分"映射；B 用锁定口径(max_seq_len=32768)重测本地 baseline 锚。
- roadmap.md 加指针、00-index 加索引。未动任何队友工作副本；隔离 worktree 基于 origin/main → push main。

## ~晚 Claude 纳入 2 份 gfx936 实测讲义

- 用户提供 2 份 DCU 微基准讲义：《decode访存瓶颈与双缓冲_DCU实测》(expA.py/dbocc.hip)、《第三集·带宽利用与算子优化》。
- 认定第一份就是 memory/50 已引用「外部 gfx936 作业」的原始出处（HBM 1206/395TFLOPS/拐点327 出自此）；第二份是新的带宽利用率方法论。
- **三个增量已落 memory/50 新小节 + plans/r2-r5-detail**：
  1. TPOT 三段分解 45(权重下限)/49(讲义纯前向)/69(本仓端到端P99) → kernel 级已到顶，**~20ms 是 host 开销** = R2 的精确靶子（decode 带宽侧仍不可动）。49 vs 69 不同源，R2.0 trace 需证实。
  2. GQA 6:1 复用（heads=24/kv_heads=4）→ R3.1 flash-attn kernel 要吃满；GDN 参考库 FLA(flash-linear-attention) → R3.2 对标；FlashAttention → R3.1 范本。
  3. inductor combo_kernels 已开（vllm_server.log 证），R2/R3 手融前先查已融了啥。
- **拦下一个红线陷阱**：两份讲义最终药方"权重 int8/int4 量化把 TPOT 49→40ms"在本赛题 = 持久化量化红线，违规清零，已在 memory/50 与 r2-r5-detail 双处标红「分析可用、药方不可抄」。
- 未动队友工作副本；隔离 worktree 基于 origin/main → push main。

## ~晚 Claude R2.1 任务卡

- 产出 `plans/task-r2.1-cudagraph.md`：R2.1 CUDA graph 覆盖度单页可执行任务卡（供 Codex/GLM 直接照做）。
- 核心诊断 = **enforce_eager 对照**（关全部 graph 跑守门员）→ 直接量化"graph 现在帮了多少"，附决策树：关后变慢=graph已生效剩余空间小；关后几乎不变=graph在decode没生效、~20ms host 几乎全可抢。
- 含开关矩阵（A现状/B enforce_eager诊断/C提升覆盖度）、判定标准、验收门槛、红线自查、产出物、交回一句话。
- 00-index 加「任务卡」小节指针。未动队友工作副本；隔离 worktree → push main。

## 15:43-16:30 Codex R2.1 enforce_eager A/B

- Extended `scripts/guard_bench.py` with diagnostic `--enforce-eager`, gated behind `--locked-start-script`; generated locked start scripts now pass `--enforce-eager` only when requested and guard artifacts collect `vllm_server.log` / `start_vllm_locked.sh` by default.
- Ran arm A `experiments/r2-eager-a-baseline-8to16-20260707-1543/` on job `656918`: locked `runai_streamer`, `max_seq_len=32768`, 8-16K only, 3 prompts x 3 reps, accuracy none. Median output `7.882584 tok/s`, TTFT-P99 `13225.199ms`, TPOT-P99 `69.910539ms`.
- Arm A log evidence: `vllm_server.log:20` shows `enforce_eager=False` and `cudagraph_mode FULL_AND_PIECEWISE`; `vllm_server.log:129-131` shows mixed prefill-decode `PIECEWISE` graph capture and decode `FULL` graph capture completed.
- Ran arm B `experiments/r2-eager-b-enforce-8to16-20260707-1609/` with the same protocol plus `--enforce-eager`. Median output `5.737616 tok/s`, TTFT-P99 `13374.594ms`, TPOT-P99 `118.195300ms`.
- Arm B log evidence: `vllm_server.log:19` says eager disables torch.compile and CUDAGraphs; `vllm_server.log:24` confirms compile mode `NONE`, `cudagraph_mode NONE`, and `max_seq_len=32768`.
- Verdict saved in `experiments/r2-eager-enforce-ab-20260707/`: eager is `+48.285ms` TPOT-P99 and `-27.21%` output throughput vs current. Current decode is not graph-missing; R2.1 should shift to decode-only launch-count confirmation and narrow residual host-overhead tuning. Caveat: this measures graph+compile-stack contribution, not pure CUDAGraph-only contribution.
- Cleanup: stopped the eager server after the run; `http://127.0.0.1:8001/health` returned `000`.

## ~晚 Claude R2.1 决策 + R2.0/R3.1 任务卡

- 消化 codex R2.1 enforce_eager 结论：graph 已 FULL、省 48ms（69.9→118ms），覆盖度无肉 → **R2.1 关闭**。
- 战略判断（算账）：decode 侧满额天花板仅约 +7 分（实际更少、越来越难啃）；prefill 是 80% 权重胜负手 → **主力转 R3**。
- 产出 2 张任务卡：
  - `task-r2.0-decode-profile.md`（小，收尾 R2）：定性剩余 ~20ms 构成（host vs 段间 vs kernel忙时）+ full-attn decode 占比（判断 R3 能否一箭双雕）。不占 GPU 主线。
  - `task-r3.1-flash-attention.md`（主攻）：full-attn prefill flash-attention on gfx936；η 体检先行 + head_dim=256 正确性硬门槛 + 先正确后快 + GQA 6:1 复用 + 优先复用成熟实现别手写。
- r2-r5-detail：R2.1 标注已诊断关闭（留档原调查）；执行顺序更新为"R2.0 profile + R3.1 并行"。00-index 补两张卡指针。
- 口径观察：locked baseline 8-16K=7.88（旧 7.23 作废）。未动队友工作副本；隔离 worktree → push main。

## ~晚 Claude R3.1 卡改「直接做」版

- 用户定调：直接做 R3，别设验证闸门。
- 采纳：去掉 R3.1 的 η 前置体检闸门（改并行测、当消融证据，不阻塞动手）；执行 5 步压成 3 步。
- 保留唯一不可省的验证：**Step 2 tiny-tensor 数值对拍（5 分钟）**——理由是 attention 算错会「假性变快」（吞吐涨但精度崩），不对拍就提交=精度四类清零、总分暴跌。对拍不过的吞吐一律不采信。
- 只改 task-r3.1（执行层）；设计层 r2-r5-detail §3.1 不动。隔离 worktree → push main。

## ~晚 Claude 整合 opus4.8 web-grounded 审计

- 用户把 R2–R5 方案交 opus4.8 审计（web 搜索 grounding vLLM/ROCm 事实），产出高质量修正。逐条分类：采纳净增量 / 被本仓实测超越 / 审计自认待本地验证，存档 `plans/audit-opus-20260707.md`。
- **被实测超越**：审计"enforce_eager 会误导需三方对照" → 本仓 R2.1 日志实锤 decode=FULL graph（非 piecewise 误判），graph 省 48ms、R2.1 已关闭；"第二次提交=R2 cudagraph"作废 → 改"第二次提交=第一个真优化"。
- **净增量已整合**：
  - memory/50：MTP=违禁杠杆；issue #35238（DeltaNet dtype/FP8 乱码）→ output-equivalence gate 必做；FP8 MFMA throughput 未确认（推翻早先"FP8 支持确认"乐观）；INT8 MFMA 合规候选（待测）；gfx936 CK/Triton FA head_dim=256 build 待验证；prefill GEMM 仅 ~33% 效率。
  - r2-r5 §4：FP8→INT8（合规 prefill 上行、悬崖门控、temp=0 误差系统性）；§3 新增 R3.0 config/autotune 前置；§3.1 dump backend + head_dim=256 改"enablement 非绿地"+ 量级 1.5–2.5×；§3.2 FLA 一等风险；§0 MTP；§6 战略 A 改真优化。
  - task-r3.1：Step1 dump backend 并行诊断 + 启用/移植定调；Step2 加端到端 output-equivalence gate（#35238）；量级 1.5–2.5×。
  - task-r2.0：步骤 2 加 GDN state/norm/gating 非 GEMV slack + V1 detokenize 异步确认。
- 未动队友工作副本；隔离 worktree 基于 origin/main → push main。

## ~晚 Claude 执行纪律 + 本地 main 工作区

- 用户反馈"计划书应直接改本地、别绕 GitHub"。诊断：本地 meta 三个 worktree 全是 codex 分支、无 main checkout，本地 main 引用还停在 41cbafb（旧）→ 用户本地确实没有"停在最新 main"的干净视图。
- 修：建**常驻 main 工作区** `/Users/keynary/Code/xiandaobei/meta-main`（`worktree add -B main origin/main`），与 codex 的 meta-r0-r1 / meta-infra-verify 并列。以后计划书直接在此看/改，一步 push，不再用临时 worktree。未动 codex 三分支的脏改动。
- 澄清：push GitHub 非多余——codex/glm 在服务器 clone，必须经 main 中转才能 pull 到；这是 AGENTS.md 设计之初的同步机制。
- 落盘 `execution-discipline.md`（无人值守远程执行纪律，用户经 opus4.8 分析 Codex 执行提炼）：0 无人值守（不等确认/被阻塞落 handoff 切下一件）；1 长任务 tmux/nohup 脱离 + 低频轮询 ≥5min；2 子任务外包 codex exec headless 只读 verdict；3 汇报少而准。00-index 顶部加"执行 agent 先读"指针。

## 21:28-22:08 Codex GitLab 卡死后的本地闭环推进

- GitLab/官方评测不可用，用户要求先推进本地可闭环任务；Codex 进入目标模式。
- `plans/task-r3.2-gdn-prefill.md` 落盘并推 main：R3.2 下一步是 R3.1 后重抓 prefill profile，再做 GDN chunked-prefill shape dump + config/autotune spike，避免沿用 R3.1 前旧热点图盲改。
- `scripts/decode_stream_profile.py` 落盘并推 main：streaming chat 请求首 token 后用 `hipprof --session-client --start/--stop/--flush` 抓稳态 decode 窗口。
- R3.1 full accuracy 本地回归完成：`experiments/r3-fa-candidate-fullacc-20260707-2130/`，复用 locked R3.1 服务（`max_seq_len=32768`, `runai_streamer`, `XDB_R31_FLASH_ATTN_PREFILL enabled`）。Local full scores：hotpotqa `77.96`, gov_report `32.71`, retrieval `100.00 (30/30)`, aggregation `100.00 (30/30)`；无精度清零。
- R2.0 decode-only profile 完成：`experiments/r2-decode-profile-r31-20260707-2151/`。64 个稳态 decode chunk：wall `4553.406ms` (`71.147ms/chunk`), kernel busy `4453.442ms` (`69.585ms/chunk`), residual only `1.562ms/chunk`; `hipGraphLaunch=64` for 64 chunks；full-attn decode `kernel_unified_attention_3d` only `4.158%` (`2.893ms/chunk`)。
- Decision: R2.3 host overlap 降级，decode 侧无大肉；主线转 R3.2 post-R3.1 prefill profile + GDN chunked-prefill。

## 22:10-22:30 Codex R3.2 Step0 post-R3.1 prefill profile

- 用户指出 R3.1 full-accuracy 本地验收已经跑过；Codex 不重复启动 full accuracy，只继续收尾 R3.2 Step0。
- `experiments/r3.2-post-r31-prefill-profile-20260707-2210/`：R3.1 source on (`847d1bef10b0b5bb71b7e427535b610a20a4d263`), locked `max_seq_len=32768`, `load_format=runai_streamer`, `XDB_R31_FLASH_ATTN_PREFILL enabled`。Warmup 后用 hipprof 抓一个 8-16K prefill request (`13964` prompt tokens, `max_tokens=1`)。
- Result: request wall `3299.468ms`; hipkernel total busy `3163.713ms` (`95.886%` of wall); residual `135.755ms`。`Cijk_*` Tensile GEMM dominates `67.101%`; flash-attn prefill is `8.994%`; old unified full-attn path is absent (`0%`)。
- GDN result: core chunk kernels only `9.303%` (`chunk_gated_delta` `5.050%`, `chunk_fwd_o` `2.231%`, `recompute_w_u` `1.540%`, `chunk_scaled_dot_kkt` `0.482%`); with helpers (`_causal_conv1d`, merge, l2norm, gating) total `13.277%`。
- Decision: R3.2 GDN is lower ROI than the pre-R3.1 `~30%` assumption. Update `memory/50`, `plans/r2-r5-detail.md`, and `plans/task-r3.2-gdn-prefill.md`: next mainline should be R3.0/R2.4 GEMM library/autotune unless a 16-32K post-R3.1 profile contradicts this.

## 22:48-22:58 Codex R3.0/R2.4 GEMM autotune setup

- 用户要求继续下一步并进入目标模式。Codex 以 `plans/r2-r5-detail.md` 最新结论为准：R3.2 已降级，下一主线改 R3.0/R2.4 GEMM library/autotune。
- Remote survey `experiments/r3.0-gemm-autotune-survey-20260707-2249/`: container job `658058`, source repo `847d1bef1`, device `BW/gfx936`, PyTorch `2.10.0`, HIP `6.3.26093`, vLLM `0.18.1`。No vLLM service was running; this was toolchain-only.
- Survey result: standalone GEMM tuning CLIs are not exposed (`hipblaslt-bench`, `rocblas-bench`, `rocblas-gemm-tune`, Tensile clients, `omniperf` all not found); only `hipprof` is available.
- PyTorch path: `torch.cuda.tunable` exists (`enable`, `tuning_enable`, `set_filename`, `tune_gemm_in_file`); Inductor `max_autotune`, `max_autotune_gemm`, and `search_autotune_cache` are currently `False`.
- Decision: create `plans/task-r3.0-gemm-autotune.md`. Next implementation should first attribute `Cijk_*` kernel rows to exact ops/shapes, then run env/config-gated TunableOp or Inductor autotune A/B under locked `guard_bench.py`; no GEMM kernel writing.
