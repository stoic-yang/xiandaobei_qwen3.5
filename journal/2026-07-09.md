# 2026-07-09

## R3.0 Inductor A/B on fresh 27B container

- Container `Instances_2607090940238205_0_0` / Slurm job `661607` was attached. `scnetctl.py` needed a fallback because node-side `docker inspect` returned permission denied; it now reads platform `_dockerlist_<job>` files for `IPADDR=` and uses the generated SSH config for login-side commands.
- Preflight showed `8001` and `18001` closed, no vLLM processes, no `/root/Qwen3.5-27B`, and HCU memory use `0%`; both benchmark runs used isolated port `18001`.
- Baseline `experiments/r3.0-baseline-8to16-port18001-20260709-0952/`: valid locked `runai_streamer`, `max_seq_len=32768`, 8-16K 3 prompts x 3 reps, median output `8.096026 tok/s`, TTFT-P99 `12779.450ms`, TPOT-P99 `69.743966ms`.
- Inductor candidate `experiments/r3.0-inductor-8to16-port18001-20260709-1024/`: `TORCHINDUCTOR_MAX_AUTOTUNE=1`, `TORCHINDUCTOR_MAX_AUTOTUNE_GEMM=1`, config dump proves `max_autotune=true` and `max_autotune_gemm=true`; same protocol median output `8.091543 tok/s`, TTFT-P99 `12787.138ms`, TPOT-P99 `70.015290ms`.
- Verdict directory `experiments/r3.0-inductor-ab-20260709/`: candidate delta is output `-0.055%`, TTFT-P99 `+0.060%`, TPOT-P99 `+0.389%`. Stop-loss: do not expand to three buckets, do not run accuracy, do not enter defaults.
- Cleanup check after both runs: no vLLM/guard processes, ports `8001/18001` closed, HCU memory use `0%`.

## R3.1b Step0 source attribution

- After user asked to continue while teammates may push concurrently, `meta-main` was pulled and SCNet status checked. There was no running container job at `2026-07-09T15:40:08+08:00`, so no GPU A/B was launched.
- Login-node read-only check showed `vllm_cscc_competition` clean except untracked `tmp_sync_bundle/`, branch `contest-p1-ffn-pool-20260621`, HEAD `4653a56d5 perf(qwen3.5): disable mono prefill by default`.
- R4.1 remote run artifacts were pulled into `experiments/r4.1-prefill-16to32-profile-20260709-1120/raw/`: `driver.log`, `repo_status.txt`, `repo_log.txt`, `overlay_manifest.txt`.
- Source attribution: current `triton_attn.py` checks `VLLM_TRITON_FUSED_PREFILL` before `XDB_R31_FLASH_ATTN_PREFILL`; the `16-32K` profile `_fwd_kernel` matches `vllm/v1/attention/ops/triton_flash_prefill.py`, introduced by `f014885bc`. Step1 should benchmark fused-on current default vs fused-off xdb varlen before trying package flash-attn autotune.

## R3.1b fused prefill A/B launch

- User asked to continue until a new growth point toward 80. Chrome SCNet console showed stopped `Instances_2607090940238205`; Codex started it via the existing console session. `scnetctl.py attach` found Slurm job `662724`, container `662724_e03r1n10`, IP `173.0.58.7`.
- Preflight: `vllm_cscc_competition` HEAD `4653a56d5`, only untracked `tmp_sync_bundle/`; ports `8001/18001` down; no vLLM/hipprof/benchmark processes; `hy-smi` HCU use `0%`, memory use `0%`.
- `scripts/guard_bench.py` overlay list was extended to include `vllm/v1/attention/ops/triton_flash_prefill.py` so R3.1b measures current source rather than a stale wheel copy.
- Detached launcher `experiments/r3.1b-fused-ab-20260709/run_ab.sh` started local PID `63192`; A arm `r3.1b-fused-A-default-16to32-20260709-1605` remote guard PID `119` is running on isolated port `18001`. B arm will run after A: `VLLM_TRITON_FUSED_PREFILL=0`, `XDB_R31_FLASH_ATTN_PREFILL=1`.

## R3.1b fused prefill A/B result

- The original local launcher died before collecting A, so Codex manually pulled A artifacts and launched B with a robust wrapper. No shared `8001` service was touched.
- Same-container 16-32K A/B, locked `runai_streamer`, 3 prompts x 3 reps:
  - A fused default: `experiments/r3.1b-fused-A-default-16to32-20260709-1605/summary.json`, output `4.122288 tok/s`, TTFT-P99 `21793.173ms`, TPOT-P99 `71.514ms`.
  - B xdb/package varlen: `experiments/r3.1b-fused-B-xdbvarlen-16to32-20260709-1605/summary.json`, output `4.297041 tok/s`, TTFT-P99 `19279.477ms`, TPOT-P99 `71.532ms`.
  - B delta: `+4.239%` output, `-11.535%` TTFT; TPOT unchanged.
- Global B three-bucket check `experiments/r3.1b-xdbvarlen-3bucket-20260709-1700/summary.json`: `8.021708 / 8.175304 / 4.294348 tok/s` for `4-8K / 8-16K / 16-32K`. Verdict: global `VLLM_TRITON_FUSED_PREFILL=0` is not a safe default; pursue threshold routing only.
- Prepared threshold candidate in isolated remote worktree `/public/home/xdzs2026_c166/vllm_cscc_codex_r31b_threshold_20260709_173633`, commit `b82c6a4a6fef63c276b982194f8baee3bd91a7f9`; patch anchor `experiments/r3.1b-fused-ab-20260709/threshold-varlen.patch`. New env `VLLM_TRITON_FUSED_PREFILL_MAX_SEQ_LEN` defaults to `0` (current behavior); setting `16384` keeps short/mid on fused and routes long prefill to xdb varlen.
- Threshold validation run `experiments/r3.1b-threshold16384-3bucket-20260709-1739/` failed to start due free-memory gate, not due candidate behavior: a separate `Qwen3.5-0.8B` service on port `8001` was running, leaving `59.54/63.98 GiB` free versus locked `0.95` requirement `60.79 GiB`. Codex did not kill the service. Next clean 27B container should run threshold `16384` three-bucket guard first.
