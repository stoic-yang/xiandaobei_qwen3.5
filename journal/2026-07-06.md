# 2026-07-06

## 01:15-01:32 Codex automation branch

- Created local branch `codex/scnetctl-baseline-auto-20260706` for automation
  work, without touching shared vLLM source checkouts.
- Added `meta/scripts/scnetctl.py` plus `meta/automation/config.json` and
  `baseline-smoke` / `baseline-full` task specs.
- Verified `status` can resolve running Slurm job `653750`, node `e03r1n07`,
  and container IP `173.0.59.7`.
- Verified `attach` writes `~/.ssh/xiandaobei.generated.conf` and can SSH to the
  container through `xiandaobei-worker-auto`.
- Added retries around flaky `zzeshell` login-node SSH behavior; observed both
  command timeout and banner-exchange timeout modes.
- Dry-ran `baseline-smoke`; no remote install/server/benchmark was executed.
- Noted concurrent OpenCode work: current automation refuses to run when active
  vLLM/benchmark/OpenCompass processes are present, and run artifacts go under
  `/public/home/xdzs2026_c166/codex_runs/<run_id>/`.

## ~20:00 Claude 设计会话：架构判断 + 分轮次计划书

- 通读 meta 全量（memory 00–40、journal、8 个 experiments/summary、automation runbook）
  + `codex-full10-*/vllm_server.log`。
- **关键判断（写入新文件 `memory/50-arch-bottleneck.md`）**：
  - Qwen3.5-27B = 混合 Gated-DeltaNet 线性注意力 + 少量 full-attention（+可能 MoE）。
    证据：`gdn_attention_core`/`linear_attention`/`mamba page size`/`Mamba cache blocks`
    + TPOT 几乎不随上下文变化（69→72ms while 长度×4）。
  - 瓶颈 = **长上下文 Prefill/TTFT**（8-16K/16-32K 两档 TTFT 占 56%/68%，prefill 仅 ~758 tok/s），
    **不是** decode、**不是** KV Cache（KV 仅 6.76GB，显存有余；fp8kv 已验证不值得）。
  - 推翻上一轮纯文档预判的"KV 量化是主战场"。
  - 当前 `d29e9db3` 官方 59.0018 = 净负优化；"前9名反推 baseline"不可当锚点。
- **产出 `plans/roadmap.md`**：R0 探针 → R1 止损回退 → R2 普惠 → R3 算子重写 → R4 量化 → R5 合规。
  建议先只做 R0+R1（建守门员协议 + 止损站稳 60）。R0.1 `cat config.json` 定是否 MoE 第一个做。
- 未动任何队友工作副本；本次仅新增 meta 文档（隔离 worktree 基于 origin/main → push main）。
- 交给 Codex/GLM 的待确认项见 roadmap 末表（EM/F1、max_tokens、是否MoE、干净baseline官方分、maoym profile）。

## 19:44-21:40 Codex R0 probe + guard harness

- Created clean meta worktree `meta-r0-r1` on branch `codex/r0-r1-probe-20260706` from `origin/main`, leaving the dirty original meta checkout untouched.
- R0.1/R0.2 live probe on SCNet job `655597`: config confirms 64 layers = 48 `linear_attention` + 16 `full_attention`, **not MoE**, `head_dim=256`, `num_key_value_heads=4`, `hidden_size=5120`; device is `BW` / `gfx936:sramecc+:xnack-`, 80 CU, HIP 6.3, 64 GiB-class VRAM, DTK FP8 DU-MMA API present.
- Added guard script `scripts/guard_bench.py` and probe artifact `experiments/r0-probe-20260706-1944/`; committed as `3a21b22 exp: r0 probe guard harness`.
- Started current-HEAD guard run `guard-d29e9db3-20260706-2005` with competition wheel sha256 `a0f09295a60dc1e5f4f7e9a096f540f29165168047c3caaf37233b6e4cb8cfde`, local model copy, warmup, 3 x three throughput buckets, and full accuracy.
- Reused historical rocprofv2 profile `rocprofv2_8_16K_20260622_161546`; summary stored in `experiments/r0-profile-20260706-2138/`. Hot-window profile ranks full attention first, GDN chunk second, GEMM third; decode-only bandwidth-vs-launch remains pending.
- `guard-d29e9db3-20260706-2005` full accuracy did not finish before the manual stop-loss. Throughput completed with medians 12.2278 / 7.2299 / 4.6514 output tok/s and weighted 7.4559. A smoke `run_accuracy.sh all 1` completed for all four datasets, but full accuracy remains pending.

## 22:39 Codex R0.0 SSH multiplex gate

- Patched `scripts/scnetctl.py` so generated `~/.ssh/xiandaobei.generated.conf` writes `ControlMaster auto`, `ControlPath ~/.ssh/cm-xiandaobei-%C`, and `ControlPersist 10m` into both `xiandaobei-login` and `xiandaobei-worker-auto` Host blocks.
- Ran `python3 -m py_compile scripts/scnetctl.py` once; passed.
- Ran `python3 scripts/scnetctl.py attach`; regenerated config for job `655597`, node `e03r2n07`, container IP `173.0.148.8`.
- Verified reuse with generated config: first `ssh ... true` `real 15.69`, second `real 0.05`; `ssh -F /Users/keynary/.ssh/xiandaobei.generated.conf -O check xiandaobei-worker-auto` returned `Master running`.
- Note: bare `ssh -O check xiandaobei-worker-auto` does not load the generated config on this Mac, so project remote commands must use `-F /Users/keynary/.ssh/xiandaobei.generated.conf` or `scnetctl` helpers.
- Cleaned up an interrupted pre-R0.0 overlay benchmark: killed residual `run_throughput`/vLLM server and restored site-packages with `pip install --force-reinstall --no-deps vllm_cscc_competition/dist/*.whl`.

## 22:52-23:20 Codex R1 wheel fingerprint + a55 screening

- Ran `guard-a55f3c3-overlay-8to16-20260706-2252`, a screening-only 8-16K x3 test after overlaying selected Python files from `a55f3c3`. Median 8-16K output throughput was 7.245884 tok/s, smoke accuracy all-1 completed. This is not a full R0.3 guard.
- Fingerprinted remote source git blobs, wheel zip entries, and installed site-packages in `experiments/r1-wheel-fingerprint-20260706-2320/`.
- Critical correction: competition repo HEAD is `d29e9db3f`, but `dist/*.whl` / site-packages do not include the d29 `triton_unified_attention.py`; that file matches `a55f3c3` and earlier. Therefore `guard-d29e9db3-20260706-2005` is a measurement of installed wheel content, not a real d29 source-path measurement.
- Restored site-packages with `pip install --force-reinstall --no-deps vllm_cscc_competition/dist/*.whl` after the overlay, and verified no benchmark/server process remained.
- Updated `scripts/guard_bench.py` so future guard runs collect `runtime_fingerprints.json` from the installed wheel and site-packages; `python3 -m py_compile scripts/guard_bench.py` passed and dry-run showed the fingerprint artifact path in the generated remote script.
- Hardened `scripts/guard_bench.py` for R1 matrix execution: added `--overlay-rev` and `--buckets`, added overlay manifest collection, and verified with `python3 -m py_compile` plus `bash -n` on `experiments/r1-guard-overlay-runner-20260706-2350/raw/dryrun_overlay_8to16.sh`.
